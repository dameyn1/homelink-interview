{
  "name": "device-service",
  "version": "1.0.0",
  "description": "IoT device service with REST API, MQTT ingestion, and MongoDB storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test-devices": "node virtualDevices.js",
    "announce-devices": "node virtualAnnounce.js",
    "docker-build": "powershell -Command \"docker compose down; docker compose build --no-cache; docker compose up -d\""
  },
  "dependencies": {
    "axios": "^1.15.1",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongoose": "^9.4.1",
    "mqtt": "^5.15.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
